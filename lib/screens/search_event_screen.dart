import 'package:flutter/material.dart';

class SearchEventScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Search Event not yet Implemented'),
    );
  }
}
